#pragma once

#include <glw/utils/math/constants.hpp>
#include <glw/utils/math/basics.hpp>

namespace glw::math {
    // DEG to RAD constants

    /// @brief Equivalents 30 degrees to rad
    constexpr float DEG30 = glw::math::Rad(30.0f);
    /// @brief Equivalents 45 degrees to rad
    constexpr float DEG45 = glw::math::Rad(45.0f);
    /// @brief Equivalents 60 degrees to rad
    constexpr float DEG60 = glw::math::Rad(60.0f);
    /// @brief Equivalents 75 degrees to rad
    constexpr float DEG75 = glw::math::Rad(75.0f);
    /// @brief Equivalents 90 degrees to rad
    constexpr float DEG90 = glw::math::Rad(90.0f);
    /// @brief Equivalents 120 degrees to rad
    constexpr float DEG120 = glw::math::Rad(120.0f);
    /// @brief Equivalents 160 degrees to rad
    constexpr float DEG160 = glw::math::Rad(160.0f);
    /// @brief Equivalents 180 degrees to rad
    constexpr float DEG180 = glw::math::Rad(180.0f);
    /// @brief Equivalents 270 degrees to rad
    constexpr float DEG270 = glw::math::Rad(270.0f);
    /// @brief Equivalents 360 degrees to rad
    constexpr float DEG360 = glw::math::Rad(360.0f);
}
