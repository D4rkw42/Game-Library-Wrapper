#pragma once

namespace glw {
    /// @brief Initializes GLW Library
    /// @return `true` if successfully initialized
    bool Init(void);
    /// @brief Finishes GLW Library   
    void Shutdown(void);
}
